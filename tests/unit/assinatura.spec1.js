describe('unit plano', () => {
    /*
    before(async () => {
        await mongoose.connect('mongodb://localhost:27017/planos', { useNewUrlParser: true, useUnifiedTopology: true });
        const service = new PlanoService();
    });

    describe ('get-plano', () => {

        it('get plano', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            const planoSalvo = this.service.create(plano);
            const resultadoFind = this.service.findById(planoSalvo.id);

            expect(resultadoFind.id).toBe(planoSalvo.id);
        });
    });

    describe ('criar-plano', () => {

        it('Deve criar um plano', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ nome', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ valor', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });
    });

    describe ('editar-plano', () => {

        it('Deve criar um plano', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ nome', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ valor', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });
    });

    describe ('remover-plano', () => {

        it('Deve criar um plano', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ nome', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });

        it('Criar plano s/ valor', () => {   

            const plano = new Plano({
                nome: 'Plano 1',
                valor: 100.00,
                descricao: 'Plano 1'
            });

            var resultado = service.criarPlano(plano);

            expect(resultado.nome).toBe('Plano 1');
            expect(resultado.valor).toBe(100.00);
            expect(resultado.descricao).toBe('Plano 1');
        });
    });
    */
});